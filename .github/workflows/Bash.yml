name: Test Bash

on:
  push:
    paths:
      - 'Bash/**'
      - '.github/workflows/Bash.yml'
  pull_request:
    paths:
      - 'Bash/**'
      - '.github/workflows/Bash.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repo
      uses: actions/checkout@v2

    - name: Set up dependencies
      working-directory: Bash
      run: |
        wget https://github.com/SamirTalwar/smoke/releases/download/v2.3.0/smoke-v2.3.0-Linux-x86_64
        chmod 755 smoke-v2.3.0-Linux-x86_64
        alias smoke=smoke-v2.3.0-Linux-x86_64

    - name: Test
      working-directory: Bash
      run: ./run_tests.sh
